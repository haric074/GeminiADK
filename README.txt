🧠 Google ADK Local Setup – Jupyter Notebook

A complete working setup for the Google Agent Development Kit (ADK) using Gemini models on your local Jupyter Notebook.

✨ Features

Build and run a fully functional AI Agent with Gemini

Supports both local & Kaggle environments

Includes retry logic for quota (429) errors

Launch ADK Web UI locally with a single command

Interactive querying with Google Search integration

🧰 Requirements
pip install google-adk google-genai

🚀 Quick Start
from adk_local_agent_setup import ask
import asyncio

await ask("What is Agent Development Kit from Google?")

🌐 Launch ADK Web UI
adk web --port 8000


Then click the button generated by:

show_adk_button()
